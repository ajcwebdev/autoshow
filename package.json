{
  "name": "autoshow",
  "author": "Anthony Campolo",
  "description": "An example workflow for automatically generating customizable show notes for audio and video files.",
  "keywords": [
    "ChatGPT",
    "Claude",
    "Gemini",
    "Deepseek",
    "Fireworks",
    "Together",
    "Whisper.cpp",
    "Deepgram",
    "AssemblyAI"
  ],
  "type": "module",
  "scripts": {
    "node:base": "node --no-warnings --experimental-transform-types --es-module-specifier-resolution=node",
    "setup": "bash .github/setup/setup.sh",
    "check": "npx tsc",
    "as": "npm run node:base -- src/commander.ts",
    "test:base": "npm run node:base -- --test",
    "serve": "npm run node:base -- --watch src/fastify.ts",
    "local-test": "npm run node:base -- test/server-local.ts",
    "all-test": "npm run node:base -- test/server-all.ts",
    "prompts-test": "npm run node:base -- test/server-prompts.ts",
    "models-test": "npm run node:base -- test/server-models.ts",
    "docker-up": "docker-compose -f .github/docker-compose.yml --env-file .env up -d --build",
    "up": "docker-compose -f .github/docker-compose.yml --env-file .env up -d --build",
    "web": "npm run up && cd web && npm i && npm run dev",
    "start": "npx prisma migrate deploy --schema=/usr/src/app/prisma/schema.prisma && npm run node:base -- src/fastify.ts",
    "docker-info": "docker images && docker-compose -f .github/docker-compose.yml images && docker ps && docker-compose -f .github/docker-compose.yml ps && docker volume ls",
    "docker-inspect": "docker network inspect autoshow-network && docker inspect autoshow-service && docker inspect autoshow-postgres",
    "docker-logs": "docker logs autoshow-service && docker-compose -f .github/docker-compose.yml logs",
    "docker-down": "docker-compose -f .github/docker-compose.yml --env-file .env down --remove-orphans --volumes",
    "prune": "docker kill $(docker ps -q) 2>/dev/null || true && docker rm -f $(docker ps -aq) 2>/dev/null || true && docker builder prune -af && docker network prune -f && docker image prune -af && docker volume prune -af && docker system prune -af --volumes && docker network rm $(docker network ls -q) 2>/dev/null || true",
    "get-db": "docker-compose -f .github/docker-compose.yml exec -T autoshow-postgres psql -U myuser -d mydatabase -t -q --set AUTOCOMMIT=on --set ON_ERROR_STOP=on -c 'SELECT row_to_json(s) FROM show_notes s'",
    "wipe-db": "docker-compose -f .github/docker-compose.yml exec -T autoshow-postgres psql -U myuser -d mydatabase -t -q --set AUTOCOMMIT=on --set ON_ERROR_STOP=on -c 'TRUNCATE show_notes, embeddings RESTART IDENTITY CASCADE;'",
    "railway-info": "railway list && railway status && railway volume list && railway variables -s autoshow && railway variables -s Postgres",
    "create-data-contract": "npm run node:base -- .github/create-dash-contract.ts"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "0.39.0",
    "@deepgram/sdk": "3.11.1",
    "@fastify/cors": "11.0.0",
    "@google/generative-ai": "0.22.0",
    "@prisma/client": "6.4.1",
    "assemblyai": "4.9.0",
    "chalk": "5.4.1",
    "commander": "13.1.0",
    "dash": "4.8.0",
    "fast-xml-parser": "5.0.8",
    "fastify": "5.2.1",
    "file-type": "20.4.0",
    "openai": "4.86.1",
    "prisma": "6.4.1"
  },
  "devDependencies": {
    "@types/node": "22.13.7",
    "typescript": "5.8.2"
  }
}
